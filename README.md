Generate PDF with graphs, using Python libraries
---

# How to run

Create a Python venv if needed:

    python3 -m venv venv
    source venv/bin/activate

Install required libraries:

    pip install -r requirements.txt


Run with test data:

    python climate-report.py data.md 

# Initial prompt

The initial code was generated by Claude.ai (Sonnet 4.5)
from this prompt:


Using only open source libraries, write code that:
* Takes a Markdown formatted table as input, that contains temperature and humidity readings for a house, 12 measurements
* Generates a meaningful chart of that data, in a vector format
* Integrates the chart in a PDF page that demonstrates high quality typesetting, similar to a newspaper front page